.gradle/
build/
run/
!run/config/
# Holds the user's real YouTube session cookie -- must never be tracked,
# even if run/config/ ever gets un-ignored further or the file is moved there.
jukeraft-auth.json
*.log
.idea/
*.iml
out/
.vscode/
spotifloat/
ytmfloat/
# A git worktree for parallel 26.2 development -- lives inside this same
# checkout, must never be tracked as part of the 1.21.1 branch's own tree.
/jukecraft 26.2/
